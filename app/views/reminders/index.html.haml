%h1 Reminders

%table
  %tr
    %th Title
    %th Due After
    %th Actions
 
  - @reminders.each do |reminder|
    %tr
      %td= reminder.title
      %td= reminder.period_time_text
      %td= link_to 'Edit', edit_reminder_path(reminder) 